;; -*- mode: Lisp; -*-

(setq prj-version "0.4")

(setq prj-config
      '(("project-name" . "hexo-migrator-image-reloaded")))

(setq prj-tools
      '(("Make" "cake build" "f9")
        ("Clean" "cake clean" "C-f9")
        ("Run" "echo run what" "f8")
        ("Stop" "-e eproject-killtool" "C-f8")
        ("---" nil nil)
        ("Configure" "./configure" nil)
        ("---" nil nil)
        ("Explore Project" "nautilus --browser `pwd` &" nil)
        ("XTerm In Project" "xterm &" nil)))

(setq prj-files nil)

(setq prj-curfile nil)

(setq prj-functions nil)

